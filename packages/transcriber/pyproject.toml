[project]
name = "transcriber"
version = "0.1.0"
description = "Transcription worker (faster-whisper)"
requires-python = ">=3.13"
dependencies = [
  "faster-whisper>=1.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
